#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: is the character to be checked
 * Return: 1 if uppercase or c otherwise
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
