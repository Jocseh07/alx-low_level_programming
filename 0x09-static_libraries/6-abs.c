#include "main.h"
#include <stdlib.h>

/**
 * _abs - to compute absolute value of an integer
 * @n: no to get the absolute value for
 * Return: absolute value of the integer
 */

int _abs(int n)
{
	int m = abs(n);

	return (m);
}
